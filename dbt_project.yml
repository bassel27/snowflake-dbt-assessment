name: 'snowflake_tpch_demo'
version: '1.0.0'

profile: 'snowflake_tpch_demo'

model-paths: ["models"]
test-paths: ["tests"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  snowflake_tpch_demo:
    staging:
      +schema: raw 
      +materialized: view
    marts:
      +schema: raw 
      +materialized: table